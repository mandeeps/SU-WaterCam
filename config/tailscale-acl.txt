{
	// Define watercam tag for devices
	"tagOwners": {
		"tag:watercam": ["autogroup:admin"],
	},

  "acls": [
  // Comment out the rule allowing all connections
		{
			"action": "accept",
			"src":    ["autogroup:member"],
			"dst":    ["tag:watercam:*"],
		},
  ],

	// Define users and devices that can use Tailscale SSH.
	"ssh": [
    // allow users to access their own devices
		{
			"action": "accept",
			"src":    ["autogroup:member"],
			"dst":    ["autogroup:self"],
			"users":  ["autogroup:nonroot"],
		},

    // allow users to log into watercams
		{
			"action": "accept",
			"src":    ["autogroup:member"],
			"dst":    ["tag:watercam"],
			"users":  ["pi"],
		},
	],

	// Test access rules every time they're saved.
	"tests": [
		{
			"src":    "SPECIFIC USER ACCOUNT TO TEST WITH",
			"proto":  "tcp",
			"accept": ["tag:desktop:22", "tag:watercam:22"],
		},
	],

}
